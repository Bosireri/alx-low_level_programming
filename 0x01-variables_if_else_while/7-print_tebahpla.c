#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alpa;

	for (alpa = 'z' ; alpa >= 'a' ; alpa--)
	putchar(alpa);
	putchar('\n');
	return (0);
}
